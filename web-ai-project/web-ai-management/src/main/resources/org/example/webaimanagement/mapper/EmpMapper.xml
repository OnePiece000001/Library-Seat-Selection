<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.webaimanagement.mapper.EmpMapper">

     <select id="list" resultType="org.example.webaimanagement.pojo.Emp">
        select e.* ,d.name from emp e left join dept d on e.dept_id =d.id
        <where>
            <if test="name != null and name != ''">
                e.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null ">
                and e.gender=#{gender}
            </if>
            <if test="begin != null and end != null ">
                and e.entry_date between #{begin}  and #{end}
            </if>
        </where>
        order by update_time desc
    </select>

    <delete id="deleteById">
         delete  from emp where id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </delete>


    <resultMap id="getInfo" type="org.example.webaimanagement.pojo.Emp">
<!--        <id column="id" property="id" />-->
<!--        <result column="username" property="username" />-->
<!--        <result column="password" property="password" />-->
<!--        <result column="name" property="name" />-->
<!--        <result column="gender" property="gender" />-->
<!--        <result column="phone" property="phone" />-->
<!--        <result column="job" property="job" />-->
<!--        <result column="salary" property="salary" />-->
<!--        <result column="image" property="image" />-->
<!--        <result column="entry_date" property="entryDate" />-->
<!--        <result column="dept_id" property="deptId" />-->
<!--        <result column="create_time" property="createTime" />-->
        <result column="update_time" property="updateTime" />
        <collection property="exprList" ofType="org.example.webaimanagement.pojo.EmpExpr">
            <id column="ee_id" property="id"/>
            <result column="ee_empId" property="empId" />
            <result column="ee_begin" property="begin" />
            <result column="ee_end" property="end" />
            <result column="ee_company" property="company" />
            <result column="ee_job" property="job" />
        </collection>
    </resultMap>
    <select id="getById"  resultMap="getInfo">
        select e.*,
               ee.id ee_id,
               ee.emp_id ee_empId,
               ee.begin ee_begin,
               ee.end ee_end,
               ee.company ee_company,
               ee.job ee_job
        from emp e left join emp_expr ee on e.id=ee.emp_id where e.id=#{id}
    </select>

    <update id="updateById">
        update emp
        set
            username=#{username},
            password=#{password},
            name=#{name},
            gender=#{gender},
            phone=#{phone},
            job=#{job},
            salary=#{salary},
            image=#{image},
            entry_date=#{entryDate},
            dept_id=#{deptId},
            update_time=#{updateTime}
         where id=#{id}
    </update>

<!--    获取emp员工表的统计信息-->
    <select id="getEmpJobData" resultType="java.util.Map">
        select
            (case when job=1 then "班主任"
                 when job=2 then "讲师"
                 when job=3 then "学工主管"
                 when job=4 then "教研主管"
                 when job=5 then "咨询师"
                 else "其他" end) pos,
                 count(*) num
                 from emp group by job order by num
    </select>
    
<!--     获取emp员工表性别统计信息-->
    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if(gender=1,"男性员工","女性员工") gender,
            count(*) count from emp group by gender
    </select>
</mapper>