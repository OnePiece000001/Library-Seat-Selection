1. **修改FloorTool.vue组件**：

   * 为功能菜单（创建横桌、纵桌、椅子）添加独立的点击事件处理

   * 将功能菜单的点击事件与楼层选择事件分开

   * 触发自定义事件，传递创建类型（如'create-desk1'、'create-desk2'、'create-seat'）

2. **修改LibMap.vue组件**：

   * 为FloorTool组件添加事件监听器，处理创建请求

   * 实现createDesk方法，用于创建桌子

   * 实现createSeat方法，用于创建椅子

   * 生成唯一ID的逻辑

   * 实现命名规则：desk\_name = 'D' + floor\_no + desk\_no，seat\_name = 'C' + floor\_no + seat\_no

   * 设置初始位置为100px

   * 确保新创建的物品floor\_no与当前currentFloor相同

3. **实现ID和编号生成逻辑**：

   * 为新创建的桌子生成唯一ID

   * 为新创建的座位生成唯一ID

   * 生成新的desk\_no和seat\_no，确保唯一性

4. **确保事件绑定正确**：

   * 新创建的桌子和椅子会自动继承组件的事件处理（拖动或点击）

   * 因为使用的是v-for渲染，新添加的物品会自动应用SeatVue和DeskVue组件的事件绑定

5. **测试功能**：

   * 点击创建纵桌，验证生成desk1样式的桌子

   * 点击创建横桌，验证生成desk2样式的桌子

   * 点击创建椅子，验证生成椅子

   * 验证新创建的物品floor\_no与当前currentFloor相同

   * 验证命名规则正确

   * 验证初始位置为100px

   * 验证拖动和点击事件正常工作

