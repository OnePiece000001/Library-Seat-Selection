1. **修改SeatVue.vue组件**：

   * 添加`mapState`映射，从vuex获取`usertype`状态

   * 根据`usertype`条件性绑定`mousedown`事件（仅管理员可拖动）

   * 为普通用户添加座位点击事件，使用alert弹窗提示

   * 根据`usertype`动态修改鼠标样式（管理员为move，普通用户为pointer）

2. **修改DeskVue.vue组件**：

   * 确认已通过`mapState`获取`usertype`状态

   * 根据`usertype`条件性绑定`mousedown`事件（仅管理员可拖动）

   * 根据`usertype`动态修改鼠标样式（管理员为move，普通用户为default）

3. **测试不同用户类型的行为**：

   * 管理员身份：座位和桌子可拖动

   * 普通用户身份：座位和桌子不可拖动，点击座位显示alert弹窗

