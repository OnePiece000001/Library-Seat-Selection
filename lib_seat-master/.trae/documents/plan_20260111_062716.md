# 实现借阅书籍功能

## 1. 需求分析
- 点击借阅书籍按钮后弹出对话框
- 对话框中需要填写借阅时间（borrow_time）和归还时间（return_time）
- 使用mapState从vuex获取当前用户的user_name和user_phone
- 获取当前书籍的book_no、book_name信息
- 调用borrowbook接口发送数据给后端

## 2. 实现方案

### 2.1 技术选型
- Element UI 组件库：用于实现对话框、日期选择器和表单
- Vuex：用于获取当前用户信息
- Axios：用于调用后端接口

### 2.2 实现步骤

#### 步骤1：添加borrowbook接口
- 检查dept.js文件，添加borrowbook接口定义

#### 步骤2：修改BorrowCard.vue，添加借阅对话框
- 导入mapState和borrowbook接口
- 添加对话框相关的data属性
- 添加借阅时间和归还时间的表单字段
- 实现borrowBook方法，处理对话框显示和借阅逻辑

#### 步骤3：实现借阅逻辑
- 在对话框确认事件中，收集表单数据
- 从vuex获取用户信息
- 从currentBook获取书籍信息
- 调用borrowbook接口发送数据
- 处理接口返回结果，显示成功或失败提示

## 3. 代码修改计划

### 3.1 dept.js 修改点
```javascript
// 在dept.js中添加borrowbook接口
export const borrowbook = (data) => request.post('/m1/7604053-7342783-default/borrowbook', data);
```

### 3.2 BorrowCard.vue 修改点

#### 3.2.1 导入相关依赖
```vue
<script>
import { mapState } from 'vuex';
import { borrowbook } from '@/api/dept';
import { Message } from 'element-ui';

export default {
  // 原有代码
  computed: {
    ...mapState(['username', 'userPhone'])
  },
  data() {
    return {
      // 原有数据
      dialogVisible: false,
      borrowForm: {
        borrow_time: '',
        return_time: ''
      },
      rules: {
        borrow_time: [
          { required: true, message: '请选择借阅时间', trigger: 'change' }
        ],
        return_time: [
          { required: true, message: '请选择归还时间', trigger: 'change' }
        ]
      }
    }
  }
}
</script>
```

#### 3.2.2 添加借阅对话框模板
```vue
<template>
  <!-- 原有代码 -->
  
  <!-- 借阅对话框 -->
  <el-dialog
    title="借阅书籍"
    :visible.sync="dialogVisible"
    width="500px"
    @close="resetForm"
  >
    <el-form :model="borrowForm" :rules="rules" ref="borrowForm">
      <el-form-item label="借阅时间" prop="borrow_time">
        <el-date-picker
          v-model="borrowForm.borrow_time"
          type="datetime"
          placeholder="选择借阅时间"
          style="width: 100%"
        ></el-date-picker>
      </el-form-item>
      <el-form-item label="归还时间" prop="return_time">
        <el-date-picker
          v-model="borrowForm.return_time"
          type="datetime"
          placeholder="选择归还时间"
          style="width: 100%"
        ></el-date-picker>
      </el-form-item>
    </el-form>
    <span slot="footer" class="dialog-footer">
      <el-button @click="dialogVisible = false">取消</el-button>
      <el-button type="primary" @click="handleBorrow">确认借阅</el-button>
    </span>
  </el-dialog>
</template>
```

#### 3.2.3 添加借阅相关方法
```vue
<script>
export default {
  // 原有代码
  methods: {
    // 原有方法
    borrowBook() {
      this.dialogVisible = true;
    },
    
    async handleBorrow() {
      // 表单验证
      this.$refs.borrowForm.validate(async (valid) => {
        if (valid) {
          try {
            // 准备借阅数据
            const borrowData = {
              user_name: this.username,
              user_phone: this.userPhone,
              book_no: this.currentBook.book_no,
              book_name: this.currentBook.book_name,
              borrow_time: this.borrowForm.borrow_time,
              return_time: this.borrowForm.return_time
            };
            
            // 调用借阅接口
            const res = await borrowbook(borrowData);
            
            // 处理接口返回结果
            if (res.code === 200) {
              Message.success('借阅成功');
              this.dialogVisible = false;
              this.resetForm();
            } else {
              Message.error('借阅失败：' + res.message);
            }
          } catch (error) {
            console.error('借阅失败:', error);
            Message.error('借阅失败，请重试');
          }
        }
      });
    },
    
    resetForm() {
      if (this.$refs.borrowForm) {
        this.$refs.borrowForm.resetFields();
      }
      this.borrowForm = {
        borrow_time: '',
        return_time: ''
      };
    }
  }
}
</script>
```

## 4. 测试验证
- 点击借阅书籍按钮，验证对话框是否正常显示
- 填写借阅时间和归还时间，验证表单验证是否有效
- 点击确认借阅按钮，验证是否调用borrowbook接口
- 验证接口调用成功后，是否显示成功提示并关闭对话框
- 验证接口调用失败后，是否显示失败提示

## 5. 注意事项
- 确保borrowbook接口的路径和参数格式正确
- 处理接口调用的异常情况
- 添加表单验证，确保借阅时间和归还时间都已填写
- 确保归还时间大于借阅时间
- 添加适当的错误提示，提升用户体验

通过以上实现，用户点击借阅书籍按钮后，会弹出对话框填写借阅时间和归还时间，确认后会将用户信息和书籍信息发送给后端，实现了预期的借阅功能需求。