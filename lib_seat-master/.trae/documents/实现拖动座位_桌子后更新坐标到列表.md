1. 修改 dragZoom.js 工具文件：

   * 添加通用的 stopDrag 方法，支持座位和桌子的拖动停止

   * 确保方法能处理事件清理和状态更新

2. 更新 SeatVue.vue 组件：

   * 使用公共的 stopDrag 方法替换现有实现

   * 在拖动停止时触发自定义事件，将新位置传递给父组件

3. 更新 DeskVue.vue 组件：

   * 使用公共的 stopDrag 方法替换现有实现

   * 在拖动停止时触发自定义事件，将新位置传递给父组件

4. 修改 LibMap.vue 组件：

   * 为 SeatVue 和 DeskVue 组件添加事件监听器

   * 实现事件处理方法，更新 seatList 和 deskList 中的位置信息

   * 确保坐标格式正确（转换为带px的字符串格式）

5. 测试拖动功能，确保坐标能正确更新到列表中

