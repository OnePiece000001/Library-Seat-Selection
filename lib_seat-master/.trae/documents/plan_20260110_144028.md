1. **修改ShowBook.vue**：

   * 导入getallbook接口

   * 在mounted钩子中调用该接口，获取书籍信息并存放在booklist

   * 计算书籍类型数组，并传递给ShowBookAside组件

2. **修改ShowBookAside.vue**：

   * 添加props接收书籍类型数组

   * 在模板中循环渲染导航标签，固定标签保持不变

3. **实现步骤**：

   * 首先修改ShowBook.vue，导入接口并调用

   * 然后计算书籍类型数组

   * 将数组传递给ShowBookAside组件

   * 修改ShowBookAside.vue，添加props并循环渲染导航标签

4. **技术实现**：

   * 使用Vue.js的生命周期钩子mounted

   * 使用Set数据结构进行去重

   * 使用v-for指令循环渲染导航标签

   * 使用props进行父子组件通信

5. **注意事项**：

   * 固定标签"Top50"保持不变

   * 书籍类型标签需要循环渲染

   * 确保数据格式正确

   * 处理接口调用失败的情况

